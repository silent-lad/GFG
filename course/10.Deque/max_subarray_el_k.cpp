#include <bits/stdc++.h>
#include <deque>
using namespace std;

#define ll long long

int main()
{
    deque<ll> dq;

    ll n;
    ll k;
    cin >> n >> k;
    ll max = 0;
    while (n--)
    {
        ll el;
        cin >> el;
        while (dq.front() > el)
        {
        }
    }
}